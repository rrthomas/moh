\NeedsTeXFormat{LaTeX2e}

\ProvidesPackage{moh}[2018/03/15 Mother of Hydrogen style]

% First paste in asciidoc-dblatex.sty’s body, as we can’t specify multiple --texstyle arguments to dblatex
%% Just use the original package and pass the options.
\RequirePackageWithOptions{docbook}

% Sidebar is a boxed minipage that can contain verbatim.
% Changed shadow box to double box.
\renewenvironment{sidebar}[1][0.95\textwidth]{
  \hspace{0mm}\newline%
  \noindent\begin{Sbox}\begin{minipage}{#1}%
  \setlength\parskip{\medskipamount}%
}{
  \end{minipage}\end{Sbox}\doublebox{\TheSbox}%
}

% Extra MoH bits
\usepackage{titlesec}
\usepackage{tgbonum}
\titleformat{\chapter}[block]{\filcenter\huge}{\filcenter}{20pt}{\Huge}
\titleformat{\section}[block]{\huge}{\relax}{16pt}{\huge}
\usepackage{titletoc}
\titlecontents{chapter}[1.5em]{\addvspace{1em plus 0pt}\bfseries}%
{\hspace{-1.3em}}{\hspace{-1.3em}}%
{\hfill \contentspage}[\addvspace{0pt}]

% Restore default paragraph spacing
\setlength\parskip{0pt plus 1pt}
\setlength\parindent{15pt}
